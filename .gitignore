# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
/.pnp
.pnp.*

# Next.js build output
/.next/
/out/
/build

# Vercel
.vercel

# Testing / coverage
/coverage

# TypeScript build info
*.tsbuildinfo
# NOTE: next-env.d.ts is intentionally tracked in this repo (a deliberate
# divergence from the create-next-app default, which ignores it). It is left
# committed on purpose — do NOT add it here.

# Turborepo
.turbo

# Local env files (committed example envs like .env.example are kept)
.env*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
