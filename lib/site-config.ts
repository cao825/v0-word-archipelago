// Canonical production origin — the single source of truth for the site URL,
// shared by metadata (app/layout.tsx), robots (app/robots.ts) and sitemap
// (app/sitemap.ts). Confirmed live-serving the deployment; NOT the *.vercel.app slug.
export const SITE_URL = "https://wordisles.com"
